# When writing a script or program, you may need to provide private credentials.
# These SHOULD NOT be entered in source control (passwords, usernames and tokens, for example).
# Instead, enter them here, make sure that this file is included in the .gitignore file.
secret_token: 123abc
secret_password: passw0rd
secret_username: whoisthis